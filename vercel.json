{
    "version": 2,
    "builds": [
        {
            "src": "medivault-web/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/medivault-web/$1"
        }
    ]
}